fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
python-multipart==0.0.9
python-dotenv==1.0.1
pandas==2.2.3
numpy==1.26.4
sentence-transformers==3.1.1
faiss-cpu==1.8.0
google-genai==1.15.0
pymupdf==1.24.10
langchain-text-splitters==0.3.0

# Optional: only needed for the /translate-document/pdf rendering endpoint.
# Requires system libraries (see README) -- comment out if you don't need
# rendered Sinhala PDF output, and the app will still run without it.
weasyprint==62.3
